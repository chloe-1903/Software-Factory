version: "2"

services:

  bankserver:
    image: cobblestone/bankserver

    ports:
      - "9090:9090"
    network_mode: "host"

  j2e:
    image: cobblestone/j2e

    ports:
      - "8080:8080"
    network_mode: "host"
